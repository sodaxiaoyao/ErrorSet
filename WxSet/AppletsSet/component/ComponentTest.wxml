<view class="wrapper" prop-a="{{dataFieldA}}" prop-b="{{dataFieldB}}">
  <slot name="before"></slot>
  <view>这里是组件的内部细节{{ComponentProperty}}{{BehaviorProperty}}</view>
  <slot name="after"></slot>
</view>